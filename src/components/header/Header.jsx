import React from "react";

const Header = () => {
  return (
    <div className="container-header">
      <input type="text" className="text-input" />
      <button className="btn">Ricerca</button>
    </div>
  );
};

export default Header;
